import { Text, useTheme } from "react-native-paper";
import { useNavigation } from "@react-navigation/core";

PasswordReset = () => {
  const theme = useTheme();

  return <Text >Reset</Text>;
};

export default PasswordReset;
